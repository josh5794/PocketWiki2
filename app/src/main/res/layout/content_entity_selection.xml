<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".Activities.EntitySelectionActivity"
    tools:showIn="@layout/activity_entity_selection">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        android:dividerHeight="@dimen/list_item_divider_height"
        android:divider="@color/list_divider_color_light_grey"
        android:headerDividersEnabled="true"
        android:id="@+id/entityselectionactivity_lv"/>

</RelativeLayout>
